#include "InvernaderoController.h"

using namespace System::IO;
using namespace SystemControlCropController;
using namespace SystemControlCropModel;

InvernaderoController::InvernaderoController() {

}
int InvernaderoController::buscarInvernaderos(String^ id, String^ cultivo, String^ ubicacion) {
}
void editarInvernadero(String^ ubicacion, String^ suelo, String^ cultivo, int idlote, String^ sustrato, int area) {
    //List<invernadero^>^ listaInvernaderos = buscarAll();
    //for (int i = 0; i < listaInvernaderos->Count; i++) {
    //    invernadero^ Invernadero = listaInvernaderos[i];
    //    if (Invernadero->getIdinvernadero() == idMedico) {
    //        // Modificar datos del médico
    //        medico->setIdPersona(idPersona);
    //        medico->setNombre(nombre);
    //        medico->setEdad(edad);
    //        medico->setGenero(genero);
    //        medico->setDireccion(direccion);
    //        medico->setTelefono(telefono);
    //        medico->setEmail(email);
    //        medico->setEstadoCivil(estadocivil);
    //        medico->setAltura(altura);
    //        medico->setNumeroColegioMedico(colegioMedico);
    //        break; // Salimos del bucle For
    //    }
    //}
    //escribirArchivo(listaMedicos); // Escribimos los datos actualizados en el archivo

}
